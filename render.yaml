services:
  - type: web
    name: attritioniq
    runtime: python
    repo: https://github.com/YOUR_USERNAME/attritioniq
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0